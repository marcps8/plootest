export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui" }}>
      <h1>Ploot Scheduler API</h1>
      <p>Backend-only assessment — no UI required.</p>
      <ul>
        <li>
          <a href="/api/health">GET /api/health</a>
        </li>
      </ul>
    </main>
  );
}
